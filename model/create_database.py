

import sqlite3
# Create a connection to a new database file or open an existing file

def createDb(dbName):
    """
    database has already been created
    please note running this file will place products.db to the next directory up so the model folder being specified
    """
    # connection = sqlite3.connect("model/products.db")               

    # cursor = connection.cursor()
    # cursor.execute('''CREATE TABLE product (
    #                     record_identifier INTEGER PRIMARY KEY,
    #                     name TEXT NOT NULL,
    #                     use_by_date TEXT NOT NULL,
    #                     date_created TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
    #                     );''')
    
    # cursor.close()
    # connection.close()
    pass

